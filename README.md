# Código y Commandos git base para estudiantes
- git clone URL 

# Descargar cambios
- git fetch
- git pull

# Subir cambios
- git add *
- git commit -am "Descripcion"
- git push

# For merge in Ubuntu before "git pull"
- git config pull.rebase false

# Ramas
- git branch dev1
- git checkout dev1
- git push -u origin dev1
- git push origin HEAD:main

- git checkout -- .

- git checkout main
- git rebase main
# Remover si hay error la carpeta rebase-merge en \.git

# Remover todo lo de github
- Control Panel\User Accounts\Credential Manager
